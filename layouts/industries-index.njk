{% extends "layout.njk" %}
{% set waveType = 'gray' %}
{% block body %}

    <!-- Industries Section Begin -->

    <div class="industry-cstm-sec">
        <div class="container">
            <div class="ind-cstm-inr">
                <div class="indcstm-row row">
                    <div class="indcstm-left-wrp col-lg-6">
                        <div class="indcstm-left">
                            <div class="sec-inr-head">
                                <h2>Built for Your Industry, Customized for You.</h2>
                                <p>Duroair engineers industrial air filtration, ISO clean rooms, and other controlled,  clean air environments to meet your industry's niche specifications for  compliance and safety while protecting sensitive processes from contamination.</p>
                            </div>
                        </div>
                    </div>
                    <div class="indcstm-right-wrp col-lg-6">
                        <div class="indcstm-right">
                            <div class="indcstm-img indcstmimg-1">
                                <img src="/img/industry-img-1.png" alt="Industry Image">
                            </div>
                            <div class="indcstm-img indcstmimg-2">
                                <img src="/img/industry-img-2.png" alt="Industry Image">
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

  <!-- End Of Industries Section -->

  <!-- Indus Solution Section begin -->
{%- assign col1 = industries | slice: 0, 2 -%}  {# First 2 items #}
{%- assign col2 = industries | slice: 2, 3 -%}  {# Next 3 items #}
{%- assign col3 = industries | slice: 5, 2 -%}  {# Last 2 items #}

<div class="ind-solution-sec">
    <div class="container">
        <div class="ind-solution-inr has-texture">
            <div class="texture"><img src="images/curved-bg.svg" alt="Texture"></div>
            <div class="sec-head">
                <h2>Solutions for Your Industry</h2>
            </div>
            <div class="indsolution-cards-row row">

                {# First Column (2 items) #}
                <div class="indsoution-card-wrp col-lg-4">
                    {%- for industry in col1 -%}
                        {% include 'partials/industry-card.njk' %}
                    {%- endfor -%}
                </div>

                {# Second Column (3 items) #}
                <div class="indsoution-card-wrp col-lg-4">
                    {%- for industry in col2 -%}
                        {% include 'partials/industry-card.njk' %}
                    {%- endfor -%}
                </div>

                {# Third Column (2 items) #}
                <div class="indsoution-card-wrp col-lg-4">
                    {%- for industry in col3 -%}
                        {% include 'partials/industry-card.njk' %}
                    {%- endfor -%}
                </div>

            </div>
        </div>
    </div>
</div>

<!-- End of Indus Solution Section -->


    {% set grid = industries %}
    {% set gridCssClass = 'grid-3 industries-grid' %}
    {% include "partials/grid.njk" %}

{% endblock %}